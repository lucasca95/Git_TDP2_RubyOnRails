<%= form_with(model: esp, local: true) do |form| %>
  <% if esp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(esp.errors.count, "error") %> prohibited this esp from being saved:</h2>

      <ul>
      <% esp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="form-group col-sm-3">
    <%= form.label :mac %>
    <%= form.text_field :mac, id: :esp_mac, class: "form-control" %>
  </div>
</div>
<div class="row">
  <div class="form-group col-sm-3">
    <%= form.label :version %>
    <%= form.number_field :version, id: :esp_version, class: "form-control" %>
  </div>
</div>

  <div class="actions">
    <%= form.submit class: "btn btn-success" %>
  </div>
<% end %>
